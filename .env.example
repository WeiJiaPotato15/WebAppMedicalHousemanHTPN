# For one-shot scripts (seed, backup, import) run outside Streamlit.
# Copy to .env and fill in. .env is in .gitignore.

AWS_REGION=ap-southeast-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
HKJ_BACKUP_BUCKET=hkj-roster-backups

# Used only by import_from_gsheet.py
GSHEET_CSV_URL=
